version: "3.9"

services:
  hydrox:
    build: .
    container_name: hydrox
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./.git:/.git:ro
    environment:
      - HYDROX_DB_PATH=/data/hydrox.db
      - HYDROX_GIT_DIR=/.git
